{
  "name": "quill-button",
  "version": "0.0.5",
  "description": "A quill editor module that enables inserting buttons into the editor",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && babel src --presets=es2015 --out-dir dist",
    "prepublish": "yarn build",
    "watch": "watch 'yarn run build' './src' --wait=0.3"
  },
  "keywords": [
    "quill",
    "editor",
    "button"
  ],
  "author": "Adam Miller",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-env": "^1.2.2",
    "babel-preset-es2015": "^6.24.0",
    "watch": "^1.0.2"
  }
}
